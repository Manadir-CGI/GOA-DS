/**
 * @goa/design-system — the 50 Goab* React components (core, forms, layout,
 * feedback, data, navigation). Import icons, illustrations and page templates
 * from their own subpaths for tree-shaking:
 *
 *   import { GoabButton } from "@goa/design-system";
 *   import { Icon } from "@goa/design-system/icons";
 *   import { Apprenticeship1 } from "@goa/design-system/illustrations";
 *   import { StartPage } from "@goa/design-system/templates";
 *   import "@goa/design-system/styles.css";
 */
export * from "./components";
